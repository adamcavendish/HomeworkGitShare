DATAS SEGMENT
	NUM DW 0B
DATAS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS
START:
    ;INIT
	MOV AX,DATAS
	MOV DS,AX
    XOR BX,BX
    MOV AX,NUM
	CMP	AX,0H
	JZ	PRINT

TST:
	SHL	AX,1
	JNC	TST
	INC	BX ;If AX is negative, counter++
	CMP	AX,0H
	JNZ	TST
	
PRINT:
	;Print the 1s in AX
	MOV DX,BX
	ADD DX,30H
	MOV AH,06H
	INT 21H
	
	;EXIT_SUCCESS
	;SUB DX,DX
	;ADD DX,30H
	;MOV AH,06H
	;INT 21H
	MOV AH,4CH
	INT 21H
CODES ENDS
    END START








