DATAS SEGMENT

DATAS ENDS

STACKS SEGMENT
	
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
	;init
	MOV AX,DATAS
	MOV DS,AX
	;
	MOV AX, 15
	MOV BX, 10
	CWD
	DIV BX
	MOV CX, DX
	ADD AX, 30H
	MOV DX, AX
	MOV AH, 06H
	INT 21H
	MOV AX, CX
	ADD AX, 30H
	MOV DX, AX
	MOV AH, 06H
	INT 21H
	
	;end
	MOV AH,4CH
	INT 21H
CODES ENDS
    END START
